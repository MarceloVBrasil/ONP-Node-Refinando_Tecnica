const x = 5;                // ruim
const quantidadeItems = 5; // bom

const ctn = "container"        // ruim
const container = "container" // bom

const usuario = {
    info: "Titulo",
    dt: "Dados"
} // ruim


const user = {
    informacao: "Informacoes do usuario",
    dataNascimento: "data de nascimento"
} // bom


const modeloCarroString = "Modelo X" // ruim
const modeloCarro = "Modelo X" // bom

function calcular(a: number, b: number) {

} // ruim

function somar(a: number, b: number) {
    return a + b
} // bom


/**
 * Tipos de Dados
 * 
 * string - (caracteristica, a quem pertence a caracteristica)
 * number - (caracteristica, a quem pertence a caracteristica)
 * boolean - se  + Substantivo + Verbo
 */